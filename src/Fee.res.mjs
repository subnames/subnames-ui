// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Controller from "./contracts/Controller.res.mjs";

async function calculate(name, years) {
  var duration = Math.imul(years, 31536000);
  var priceInWei = await Controller.registerPrice(name, duration);
  console.log("name: \"" + name + "\", duration: " + duration.toString() + ", price: " + priceInWei.toString());
  return Number(priceInWei) / 1e18;
}

async function calculateRenew(name, years) {
  var duration = Math.imul(years, 31536000);
  var priceInWei = await Controller.rentPrice(name, duration);
  return Number(priceInWei) / 1e18;
}

export {
  calculate ,
  calculateRenew ,
}
/* Controller Not a pure module */
